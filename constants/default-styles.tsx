import { StyleSheet } from "react-native";

export default StyleSheet.create({
    bodyText:{
        fontFamily:'open-sans',
    },
    title:{
        fontFamily:'open-sans-bold',
        fontSize:18,
    }
})
//simple sytlesheets don't need to be exported; just call on the name of the file